import { Text } from "@chakra-ui/react";
import React from "react";

const ConfirmationModal = ({ description }) => {
  return <Text fontSize="16px">{description}</Text>;
};

export default ConfirmationModal;
